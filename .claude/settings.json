{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -z \"$(git status --porcelain)\" && exit 0 || npm run parallel-check:quiet:no-tests"
          }
        ]
      }
    ]
  }
}
